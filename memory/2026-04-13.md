[proactive] Mode A — Integrated PixelManager into DashboardQRList — commit f7f8dbd
[proactive] Mode A — Fixed vitest config and userEvent setup ordering — Fixed root vitest.config.ts to exclude menu-ocr tests (uses jsdom), fixed userEvent.setup() ordering in OCRProcessor.test.tsx and MenuEditor.test.tsx. Root tests now pass (20 files, 296 tests). Committed e3e769e.
[proactive] Mode A — Added server-side marketing pixel firing to redirect handler — Implemented Facebook Pixel, Google Ads, and LinkedIn Insight Tag firing in functions/r/[id].js. Pixels fire server-side when QR codes are scanned, bypassing ad blockers. All 296 tests pass. Committed f97f3c9 and pushed.
[proactive] Mode B — Scanned for open work — All tests pass (305 tests), UTM templates and marketing pixels are fully implemented. No open PRs or issues. Repo is clean and up to date.
