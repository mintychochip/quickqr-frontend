[proactive] Mode A — Added folderService with 38 integration tests — commit from earlier run
[proactive] Mode A — Added 14 unit tests for imageProxy utility functions (proxifyImageUrl, validateImageUrl, preloadImage) with comprehensive coverage for CORS proxy handling, image validation, and preloading.
[proactive] Mode A — Added database migration for utm_templates table — enables UTM template feature with RLS policies, commit 23fdd7a
